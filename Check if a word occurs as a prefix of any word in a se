
class Solution(object):
    def isPrefixOfWord(self, sentence, searchWord):
        x=sentence.split()
        for i,j in enumerate(x):
            if j[:len(searchWord)]==searchWord: return i+1
        return -1 
       
        
        
